on:
  push:
    branches:
    - "releases/**"

jobs:
  pushOnMasterLab:
    runs-on: ubuntu-16.04
    name: "lab branches:"
    steps:
      - uses: actions/npm@master
        name: docker action
        with:
          args: --version
