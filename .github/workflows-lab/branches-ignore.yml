on:
  push:
    branches-ignore:
    - "!lab-**"
    - "lab-releases/**-alpha"

jobs:
  pushOnMasterLab:
    runs-on: ubuntu-16.04
    name: "lab branchees ignore **-alpha"
    steps:
      - uses: actions/npm@master
        name: docker action
        with:
          args: --version
