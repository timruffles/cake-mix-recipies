on:
  push:
    paths:
    - "src/**"
  pull_request:
    paths:
    - "src/**"
jobs:
  pushOnMasterLab:
    runs-on: ubuntu-16.04
    name: "lab paths:"
    steps:
      - uses: actions/npm@master
        name: docker action
        with:
          args: --version
